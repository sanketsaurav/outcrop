{
	"name": "outcrop-plugin",
	"version": "0.1.0",
	"private": true,
	"description": "Obsidian plugin for Outcrop — self-hosted public note sharing",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit -p . && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"esbuild": "^0.25.0",
		"obsidian": "^1.7.2",
		"typescript": "^5.6.0"
	}
}
